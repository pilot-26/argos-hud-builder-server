I wish it's not vibecoded
I don't have a lot of time to optimize the read/write for Instrument profile, but I figure if it's just a few hundred bytes of JSON in localStorage it wouldn't hurt.
When will there be saves:
    1) When you make any changes to overlay (enable, disable, position, monitor ID...)
    2) When you make any changes to instrument (add, delete...)
    3) When you bind an axis to the instrument